适配模式，装饰模式，代理模式对比：

适配模式：让两个不兼容的类在一起工作，通常是是将类自己的接口包裹再一个已存在的类中。

    特点在于兼容，从代码层面，核心是适配的类和原有的类有相同接口，同时适配类持有原有类的引用

装饰模式：原有的方法不能满足需求，对原有的方法的一种增强操作。

    特点在于增强，代码层面被装饰类和所有的装饰类必须实现同一接口，而且必须持有被装饰的对象。

代理模式：同一个类，不直接操作目标，而是通过一个中介去操作目标类，同时还可以对目标类进行增强操作

