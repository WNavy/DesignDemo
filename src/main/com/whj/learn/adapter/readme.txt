适配器模式：主要目的是在新老接口之间进行适配，使原本由于接口不兼容而不能一起工作的类可以在一起工作

模式中的角色：
    目标接口：target客户期待的接口；目标可以是具体或抽象的类，也可以是接口
    适配的类：adaptee，需要适配的类
    适配器：adapter通过一个需要适配的对象，把原接口转换成目标对象

适配模式有两种类型：类适配器模式，对象适配器模式或者委托方式

优点：
1、复用了现存的类，解决了显存类不支持适用环境的问题。
2、将目标类和适配者类解耦
3、一个适配器可以将不同的适配者适配到同一个目标